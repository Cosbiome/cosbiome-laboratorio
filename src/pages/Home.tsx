import React from "react";
import { Typography } from "antd";

const Home = () => {
  const { Title } = Typography;

  return (
    <div className="container">
      <Title className="text-center">BIENVENIDO AL LABORATORIO DE COSBIOME</Title>
    </div>
  );
};

export default Home;
